import * as React from "react";
import { WmkLink } from "wmk-lib"; //eslint-disable-line

/**
 * @component
 * @param {Menu} topMenu
 * @param {Menu} bottomMenu
 * @param {SocialIcons} socialMedia
 * @returns {JSX}
 */
const Footer = ({ topMenu, bottomMenu, socialMedia }) => {
  return (
    <footer>
      Footer to go here
    </footer>
  )
};

export default Footer;
